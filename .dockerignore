# ---- 版本控制 / 编辑器 ----
.git
.gitignore
docs/
.vscode/
.DS_Store

# ---- 本地构建产物（不应进镜像） ----
SSE
SSE.exe
*.exe
output.txt

# ---- 运行期数据 ----
logs/

# 本地运行配置（可能含密钥，容器内配置一律由 compose 环境变量注入；
# 入库模板 config.example.yml 不受影响，可进镜像）
config/config.yml

# ---- 前端依赖/产物（后端镜像构建时不需要 node_modules 进上下文） ----
**/node_modules
vue/sse/dist

# ---- 部署辅助 ----
deploy/jenkins/jenkins_home/
